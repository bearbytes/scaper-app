import { Box } from './Box'
import { Button } from './Button'

type SideBarButtonProps = {}

export default function SideBarButton(props: SideBarButtonProps) {
  return <Button width={75} height={75} color="background" borderRadius="M" />
}
